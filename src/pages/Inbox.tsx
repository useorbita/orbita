import { ScrollArea, Title } from "@mantine/core";

export default function Inbox() {
  return (
    <ScrollArea p={"xl"}>
      <Title style={{ fontFamily: "Outfit", fontWeight: 400 }}>Eingang</Title>
    </ScrollArea>
  );
}
